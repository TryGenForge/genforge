export const metadata = {
  title: "Landing Page Copy Generator — GenForge",
  description: "Generate full landing page copy ready to publish with AI.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}