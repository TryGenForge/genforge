export const metadata = {
  title: "Call-to-Action Generator — GenForge",
  description: "Generate CTAs that turn visitors into customers with AI.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}