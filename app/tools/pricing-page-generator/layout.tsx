export const metadata = {
  title: "Pricing Page Copy Generator — GenForge",
  description: "Generate pricing page copy that makes your pricing irresistible with AI.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}