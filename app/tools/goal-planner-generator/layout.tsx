export const metadata = {
  title: "90-Day Goal Planner — GenForge",
  description: "Break your business vision into a clear 90-day action plan with AI.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}