export const metadata = {
  title: "Blog Post Intro Generator — GenForge",
  description: "Generate blog post introductions that hook readers from the first sentence with AI.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}