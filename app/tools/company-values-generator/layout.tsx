export const metadata = {
  title: "Company Values Generator — GenForge",
  description: "Generate core company values that define your culture and guide your team with AI.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}