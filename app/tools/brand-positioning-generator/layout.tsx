export const metadata = {
  title: "Brand Positioning Statement Generator — GenForge",
  description: "Create a clear brand positioning statement that sets you apart from competitors with AI.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}