export const metadata = {
  title: "Business Plan Summary Generator — GenForge",
  description: "Generate a concise business plan you can share with investors instantly with AI.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}