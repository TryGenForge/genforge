export const metadata = {
  title: "Testimonial Request Email Generator — GenForge",
  description: "Generate emails that get customers to leave reviews with AI.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}