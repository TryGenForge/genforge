export const metadata = {
  title: "Homepage Headline Generator — GenForge",
  description: "Generate converting homepage headlines for your website with AI.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}